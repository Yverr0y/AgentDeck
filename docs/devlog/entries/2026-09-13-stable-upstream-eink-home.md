# 2026-09-13 — Stable upstream membership and compact display homes

Replace telemetry-dependent upstream membership with a daemon-persisted display selection shared by macOS and Android. Initialize confirmed integrations once; preserve explicit empty lists, reconnects and absent quota. Hiding a provider does not disable its integration or session observation. Both daemon implementations expose the authenticated provider preference endpoint; vocabulary generation checks native menus and validators.

Restore square, centered single usage tanks on IPS35, 86box and Round. TRMNL places readable subscription text in a separate column beside actual usage windows, including plan-only rows. Apple previews retain real plan names/dates.

EPD47 now opens on a stable home with work/recent results beside usage. Details open deliberately; work-list selection binds to the painted session identity. Decision sends reject changed requests. NM uses a full-width durable summary and adapts the lower usage area to one through four windows without changing its stock tri-color refresh policy.

Validation: 4,482 TypeScript tests passed, one skipped; Apple Protocol/ProviderRail and Android Protocol suites passed. All six native firmware targets built, three host C++ suites passed, and real-renderer previews covered sparse and four-window data. Protocol generation and token checks are clean; clean-source design lint retains its 89 baseline findings. Host previews do not establish physical ghosting or touch-controller quality.
