# 2026-09-15 — npm 1.3.5 Windows autostart and interval metrics

Merged #336 with the contributor's original commits plus maintainer fixes for
unknown supervision, asynchronous launcher errors and Windows CI fixtures.
Merged #338's protected broadcast-attempt metrics. Release PR #340 synchronizes
all four npm packages at 1.3.5; tag source is
`fab7dec30d1f3f958d59ac80bf73621ea5d3780a`.

Build/typecheck and 4,527 tests passed, with three platform skips. Protocol
regeneration left no drift. Documentation, token and catalog checks passed;
clean-checkout design lint stayed at 89. All nine release PR checks passed,
including Windows Node 22/24/26, Linux full tests, macOS tests and iOS build.

The exact-source tarballs were installed through the real Homebrew CLI path.
Swift-only and Node-only direct Codex turns reached the Mac dashboard and the
physical Lenovo tablet. In coexistence the app attached to the sole Node
listener. After the supported Node stop, the same Swift process reclaimed 9120
from fallback 9121 by the 187-second observation; a new turn reached both screens
without restarting the app or reinstalling hooks. The installed app remained
1.3.2 local build 4; the release issue explicitly records that baseline exception
and does not claim App Store build 6801 coverage. Initial Node normalization
required one supported restart after Swift's port release; delayed port recovery
is existing behavior and does not close #327.

The final installed Node 1.3.5 serves 9120 with build `a567cfcaeb0f`; the app is a
client. The native SQLite diagnostic passes. Broadcast counters advance and a
metrics-only baseline is retained for subsequent interval monitoring; no
24-hour or per-board repaint result is claimed. The public health allow-list is
unchanged. Screenshots/logs remain private because they contain unrelated
session data. The detailed pre-tag receipt is in
[issue #314](https://github.com/puritysb/AgentDeck/issues/314#issuecomment-5678349841).

Elgato 1.3 public propagation was already verified at 12:43 KST (748.66 KB);
README and its listing now reflect that completed readback. Ulanzi remains a
separate review/publication follow-up. No companion or firmware binary changed.

All four exact 1.3.5 versions and latest tags are now public, with gitHead matching
the tested tag source. The bridge tarball integrity was verified and its CLI,
launcher, supervisor, WebSocket server and daemon server match the installed
candidate byte-for-byte. The first publishing run accepted all four uploads but
failed its 60-second shared-package readback; after all versions became visible,
only the failed job was rerun to finish the GitHub Release. The immutable tag
was not moved and no replacement version was cut.
