# 2026-09-13 — Sparse provider usage and e-ink display layout

The macOS and Android dashboard Claude upstream row was still driven by installed hooks/OAuth metadata after the quota retirement change. Gate that row on a live consumer or displayed quota; configuration metadata remains in Settings. Preserve absent usage windows in Apple hardware previews instead of converting them to zero.

IPS35, 86box and Round now let a lone usage tank occupy the former pair's width. TRMNL divides the available horizontal space between present windows, removes the permanent KEY1/KEY2 header hints, and gives recent work a labelled, roomier strip. Keep two recent rows when several sessions need two card rows. Normalize embedded control characters before fitting a single text row, retaining complete UTF-8 sequences. EPD47 renders only providers with quota, uses a full-width shorter card for a lone window, and supports secondary-only Codex usage on Focus as well as Limits.

Serial usage delivery no longer depends on Claude 5h presence and carries empty retirement snapshots. Device-info diagnostics expose held Codex 5h/7d percentages over serial and Wi-Fi, with `-1` meaning absent. This distinguishes ingestion from refresh problems without stealing a serial port.

Validation: 4,477 TypeScript tests passed, one skipped; 91 Apple Protocol/ProviderRail tests and 37 Android Protocol tests passed. Five native ESP32 targets built. Five real-renderer host previews exercised a Codex-secondary-only account; three host C++ suites passed, including newline/UTF-8 coverage. Protocol generation is clean; token mirrors agree; design lint remains at the existing 89 violations. Physical readback verifies firmware/data/repaint counters, not optical quality or ghosting. The host U8g2 shim does not rasterize Korean glyphs; UTF-8 normalization is covered by host tests and the firmware font remains unchanged.

Deployment: macOS and Lenovo installed and their actual dashboard upstream rows no longer show Claude. The Node daemon was rebuilt/restarted. EPD47, TRMNL, 86box and IPS35 were flashed over USB; Round Wi-Fi OTA completed. All five report the new firmware and Codex 7d usage (42% at final readback) with absent 5h. Round verification uses fresh Wi-Fi diagnostics, not its stale serial cache. Build hashes include pre-commit `-dirty` identities; build epochs and binary SHA-256 records in local `diagnostics/display-layout/` identify the exact artifacts.

USB findings: TRMNL's running CDC node changes to a different download node; match its USB serial identifier before choosing the new port. IPS35's inherited `no_reset` upload setting does not enter its bootloader: a default reset plus a verified 16MB flash-id allowed the write, and watchdog reset released the board into its new app. Round did not answer either USB reset probe, so deployment uses the live Wi-Fi identity. Preserve serial-suspend leases around every USB operation.
